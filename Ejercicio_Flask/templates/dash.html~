<!DOCTYPE html>
<html lang="en">
 
<head>
    <title>Python Flask Bucket List App</title>

    <script  src="../static/js/jQuery.js"></script>
 
    <script>
	$(function() {
    		$.ajax({
        	url: '/getallwish',
        	type: 'GET',
        	success: function(response) {
        		console.log(response);
			var data = JSON.parse(response);
			var itemsPerRow = 0;
			var div = $('<div>').attr('class', 'row');
			for (var i = 0; i < data.length; i++) {
     				console.log(response['Usuario'] );
 
    				if (itemsPerRow < 3) {
       
        				if (i == data.length - 1) {
            					div.append(CreateThumb(data[i].Id,data[i].Titulo, data[i].Descripcion, data[i].Path, data[i].Likes, data[i].Liked,data[i].Usuario,data[i].Fecha ));
            					$('.well').append(div);
        				} else {
            					div.append(CreateThumb(data[i].Id,data[i].Titulo, data[i].Descripcion, data[i].Path, data[i].Likes, data[i].Liked,data[i].Usuario,data[i].Fecha));
            					itemsPerRow++;}
    				} else {
        				$('.well').append(div);
        				div = $('<div>').attr('class', 'row');
        				div.append(CreateThumb(data[i].Id,data[i].Titulo, data[i].Descripcion, data[i].Path, data[i].Likes, data[i].Liked,data[i].Usuario,data[i].Fecha));
        				if (i == data.length - 1) {
            					$('.well').append(div);
        }
        				itemsPerRow = 1;
    }
     
}

		},
	        error: function(error) {
            		console.log(error);
        	}
    		});
	});
</script>
<script>

function CreateThumb(id,title, desc, filepath, like, liked, usu,fecha) {
   
    var mainDiv = $('<div>').attr('class', 'col-sm-4 col-md-4');
   
    var thumbNail = $('<div>').attr('class', 'thumbnail');
                                     
    var img = $('<img>').attr({
        'src': filepath,
        'data-holder-rendered': true,
        'style': 'height: 150px; width: 150px; display: block'
    });
   
    var caption = $('<div>').attr('class', 'caption');

    var usuario = $('<h5>').text(usu);

    var fech = $('<h5>').text(fecha);
   
    var title = $('<h3>').text(title);
   
    var desc = $('<p>').text(desc);
 
 
    var p = $('<p>');
   
    var btn = $('<button>').attr({
        'id': 'btn_' + id, 
        'type': 'button',
        'class': 'btn btn-danger btn-sm'
    });
   
    var span = $('<span>').attr({
        'class': 'glyphicon glyphicon-thumbs-up',
        'aria-hidden': 'true'
    });
    var likeSpan = $('<span>').attr('aria-hidden', 'true').html('&nbsp;' + like + ' like(s)');
    if (liked == "1") {
        if (like == "1") {
	    likeSpan.html('&nbsp;You Liked');}
        if (like > "1") {	
            likeSpan.html('&nbsp;You & ' + (like - 1) + ' Others');}
 }  

    else {
        likeSpan.html('&nbsp;' + like + ' like(s)');
}
    p.append(btn.append(span));
    p.append(likeSpan);
 
    caption.append(usuario);
    caption.append(fecha);  
    caption.append(title);
    caption.append(desc);
    caption.append(p);
 
    thumbNail.append(img);
    thumbNail.append(caption);
    mainDiv.append(thumbNail);
    return mainDiv;
 
 
}

</script>
<script>

$(document).on('click', '[id^="btn_"]', function() {
    $.ajax({
        url: '/addlike',
        method: 'POST',
        data: {
            wish: $(this).attr('id').split('_')[1],
            like: 1
        },
        success: function(response) {
            console.log(response);
        },
        error: function(error) {
            console.log(error);
        }
    });
});

</script>




    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css">
 
    <link href="../static/css/jumbotron-narrow.css" rel="stylesheet">
 
   
 
</head>
 
<body>
 
    <div class="container">
        <div class="header">
            <nav>
                <ul class="nav nav-pills pull-right">
                    <li role="presentation" class="active"><a href="#">Dashboard</a></li>
                    <li role="presentation"><a href="/Home">Principal</a></li>
                    <li role="presentation"><a href="/AddWish">Agregar Deseo!</a></li>
                    <li role="presentation"><a href="/logout">Salir</a></li>
                </ul>
            </nav>
            <h3 class="text-muted">Python Flask App</h3>
        </div>
 
        <div class="well">

	</div>
    
 
            <footer class="footer">
                <p>&copy; Company 2019</p>
            </footer>
 
        </div>
</body>
 
</html>
